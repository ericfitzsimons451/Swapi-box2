import React from 'react'
import { shallow } from 'enzyme'
import Header from '../Header/Header'

// describe('Header', () => {
//     const mockCrawlText = 'alsdkjf'
//     it('should match the snapshot with crawlText passed down', () => {
//         const wrapper = shallow(<Header crawlText={mockCrawlText} />)

//         expect(wrapper).toMatchSnapshot()
//     })
// })

//even when I change the mockCrawlText to match what the Test Suite 
//says that it is getting, it fails.  hmmmmmm